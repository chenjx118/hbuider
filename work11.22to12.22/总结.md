
```mermaid
graph LR

A[先将网页大致布局做出来]-->B（然后加入一些css的样式)
    B-->C{剩余的js部分开始在csdn上搜索以及在b站上看视频}
    C-->D(最艰难的部分)
    D-->E[下拉菜单的制作]
    D-->E[点击按钮切换页面]
    D-->E[鼠标停留在容器上会出现悬浮的说明]
    D-->E[轮播图的制作]
```

